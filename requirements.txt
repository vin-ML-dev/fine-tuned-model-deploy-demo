# Phase 1 & 2 dependencies (Google Colab T4)
# Colab already ships torch with CUDA - do NOT reinstall torch there.
transformers>=4.46.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0
accelerate>=1.0.0
pyyaml>=6.0
tensorboard>=2.17.0
